{
  "name": "form5500",
  "version": "1.0.0",
  "description": "",
  "source": "index.html",
  "homepage": "https://irenekyc.github.io/pdf-template",
  "scripts": {
    "dev": "parcel",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "rm -rf dist",
    "build": "parcel build *.html --public-url ./",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "2.9.2",
    "parcel": "^2.9.2",
    "sass": "^1.63.6"
  },
  "dependencies": {
    "gh-pages": "^5.0.0"
  }
}
